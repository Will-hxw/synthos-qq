{
  "name": "orchestrator",
  "version": "1.0.0",
  "description": "Pipeline 调度器 - 按顺序串联执行各个数据处理任务",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "dev": "node ../../scripts/devRunner.cjs --name orchestrator --entry dist/index.js --watch src --no-common",
    "dev:build": "rimraf tsconfig.tsbuildinfo && pnpm run build && node ../../scripts/redirectRequire.js && node ../../scripts/fixESMExtensions.mjs && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "dependencies": {}
}
